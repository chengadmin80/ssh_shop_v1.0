<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-mapping PUBLIC "-//Hibernate/Hibernate Mapping DTD 3.0//EN"
"http://hibernate.sourceforge.net/hibernate-mapping-3.0.dtd">
<!-- 
    Mapping file autogenerated by MyEclipse Persistence Tools
-->
<hibernate-mapping package="cn.it.shop.model">
    <class name="cn.it.shop.model.Category" table="category" catalog="shop">
        <id name="id" type="java.lang.Integer">
            <column name="id" />
            <generator class="native" />
        </id>
        <property name="type" type="java.lang.String">
            <column name="type" length="20" />
        </property>
        <property name="hot" type="java.lang.Boolean">
            <column name="hot" />
        </property>
        
        <!--需要添加 ：package="cn.it.shop.model"
        	lazy="false": 级联查询关联对象 
        	N+1问题：首先发出一条语句查询当前对象，然后发出N条语句查询关联对象，N=关联对象的不同数
        	lazy:是否加载关联对象，如果为false则及时加载，
        	fetch:什么样的方式加载关联对象，可以选择select(默认)即N+1，和join
        	注意：在may-to-one：的情况下，join是无效的，即使有效，也要配置前面两个参数
        		因为配置在xml中不灵活。
        		如果有效，a=>b=>c ，会lazy加载所有不必要的级联对象
        		所以，任何情况下，都不要在xml中配置lazy加载级联对象及fetch="join"
        	解决方案：自己写hql语句取代基于xml的配置，则可以提升性能
        -->
        <many-to-one name="account" class="Account" >
            <column name="aid" />
        </many-to-one>
    </class>
</hibernate-mapping>
